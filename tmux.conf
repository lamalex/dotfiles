# Mouse config
set -g mouse on
#set -g mouse-select-window on
#set -g mouse-select-pane on
#set -g mouse-resize-pane on

#:wq split panes using | and -
bind | split-window -h
bind - split-window -v
unbind '"'
unbind %
